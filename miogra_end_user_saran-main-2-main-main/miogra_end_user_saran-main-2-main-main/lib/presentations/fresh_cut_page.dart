import 'package:flutter/material.dart';

class FreshCutPage extends StatelessWidget {
  const FreshCutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Fresh Cut',
        ),
      ),
    );
  }
}
